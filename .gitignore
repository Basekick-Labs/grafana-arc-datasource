# Dependencies
/node_modules
/.pnp
.pnp.js
/vendor

# Testing
/coverage
*.coverprofile

# Production build
/dist

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# IDEs and editors
.idea/
.vscode/
*.swp
*.swo
*~
*.sublime-project
*.sublime-workspace

# Build artifacts
*.zip
*.tar.gz
*.md5

# Grafana specific
data/
provisioning/
grafana.db
sessions/

# OS specific
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# Go
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
go.work
go.work.sum
/bin/
# /pkg/ - Un-commented to include backend source code

# Mage
magefile

# Environment variables (NEVER commit these!)
.env
.env.local
.env.*.local
*.env

# Secrets and credentials (NEVER commit these!)
secrets/
*.pem
*.key
*.cert
credentials.json
service-account.json

# Temporary files
tmp/
temp/
*.tmp
*.bak
*.swp
.cache/

# Package manager locks (keep package-lock.json but ignore yarn.lock if using npm)
# Comment out the one you're NOT using:
# yarn.lock
# package-lock.json

# NPM
.npm
.eslintcache

# TypeScript
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# dotenv environment variable files
.env*

# Signing keys (NEVER commit!)
private.pem
public.pem

# Plugin signatures (these are generated, don't commit)
# Note: MANIFEST.txt should be committed after signing
# ci/dist/

# Local development
.local/

# Build artifacts and development files
*.backup
*.bak
src.backup/
pkg.backup/
docker-compose.yml
grafana-arc-datasource-plugin.tar.gz

# Screenshot instructions (the actual screenshots should be committed)
img/SCREENSHOTS.md
